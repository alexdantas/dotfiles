! This file makes keys do strange actions.
!
! To see them keycodes, use 'xev'
!
! xev | grep -A2 --line-buffered '^KeyRelease' | sed -n '/keycode /s/^.*keycode \([0-9]*\).* (.*, \(.*\)).*$/\1 \2/p'

remove Lock = Caps_Lock

